# c64_trial.cfg — Trial linker config for segment size measurement
#
# Used by the two-pass build to measure CODE, RODATA, BSS sizes.
# The resulting binary is not used — only the map file matters.

SYMBOLS {
    __HIMEM__:      type = weak, value = $D000;
    __BUF_FLOOR__:  type = export, value = $1500;
}
MEMORY {
    ZP:       file = "",  define = yes, start = $0002,              size = $007E;
    LOADADDR: file = %O,               start = $0801 - 2,          size = $0002;
    HEADER:   file = %O,  define = yes, start = $0801,             size = $000C;
    MAIN:     file = %O,  define = yes, start = __HEADER_LAST__,   size = __HIMEM__ - __HEADER_LAST__;
    BSS:      file = "",               start = __MAIN_LAST__,      size = __HIMEM__ - __MAIN_LAST__;
    KDATA:    file = "",               start = $F100,             size = $0EFA;
}
SEGMENTS {
    ZEROPAGE: load = ZP,       type = zp;
    LOADADDR: load = LOADADDR, type = ro;
    EXEHDR:   load = HEADER,   type = ro;
    LOADER:   load = MAIN,     type = ro,  define = yes;
    CODE:     load = MAIN,     type = ro,  define = yes;
    RODATA:   load = MAIN,     type = ro,  define = yes;
    KDATA:    load = MAIN,     type = ro,  run = KDATA, define = yes;
    DATA:     load = MAIN,     type = rw;
    BSS:      load = BSS,      type = bss, define = yes;
}
